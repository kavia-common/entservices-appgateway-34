This directory is reserved for AppNotifications plugin-specific L1 test assets/scaffolding.

- Primary L1 test sources currently live under: `entservices-appgateway-34/Tests/L1Tests/tests`
- This folder exists to mirror the per-plugin test layout used across entservices-* repositories and to host any future
  AppNotifications-specific L1 fixtures, test data files, or CMake extensions if needed.
