# Source

This folder was downloaded from:

- Repo: https://github.com/rdkcentral/entservices-infra
- Branch: `develop`
- Path: `Tests/`

Method: git sparse checkout (cone/sparse).

Note: An extra working directory (`Code_Ref/entservices-infra-tests`) was used to perform the sparse checkout, and then `Tests/` was copied into `Code_Ref/Tests/` to match the requested destination.
